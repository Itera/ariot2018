import React from 'react'
import logo from './logo.svg'

const logoStyle = {
    width: '100%',
    padding: '0',
    margin: '0',
};

const Logo = () => <img src={logo} style={logoStyle}/>;

module.exports = Logo;
